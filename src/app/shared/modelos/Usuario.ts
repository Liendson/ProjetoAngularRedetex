export class Usuario {

    // Identificação do usuário
    public id: number;
    // Nome do Usuário
    public nome: string;

   constructor(id: number, nome: string) {
      this.id = id;
      this.nome = nome;
   }
}
